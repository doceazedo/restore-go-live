name: Build and release

on:
  push:
    branches: [main]
    paths-ignore:
      - "README.md"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: release
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 11

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Clone Vencord and link plugin
        run: node scripts/setup.mjs

      - name: Embed native audio helper
        run: node scripts/embed-native.mjs

      - name: Typecheck
        run: pnpm typecheck

      - name: Build desktop
        run: pnpm build

      - name: Build browser extensions
        run: pnpm buildWeb

      - name: Package release
        run: node scripts/package-release.mjs

      - name: Publish rolling release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release delete latest --yes --cleanup-tag 2>/dev/null || true
          gh release create latest \
            --title "RestoreGoLive $(git rev-parse --short=7 HEAD)" \
            --notes "Automatic build of \`main\` (${{ github.sha }}).

          Windows:
          \`\`\`powershell
          irm https://doce.sh/restore-go-live/install.ps1 | iex
          \`\`\`

          macOS / Linux:
          \`\`\`sh
          curl -fsSL https://doce.sh/restore-go-live/install.sh | bash
          \`\`\`" \
            release/*
